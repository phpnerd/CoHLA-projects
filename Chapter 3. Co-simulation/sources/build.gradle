group 'RoomThermostat'
version '0.1'

allprojects {
    apply plugin: 'java'
}

subprojects {
    repositories {
        mavenCentral()
        flatDir {
            dirs "../lib"
        }
    }

    dependencies {
        testCompile group: 'junit', name: 'junit', version: '4.11'
    }
}
